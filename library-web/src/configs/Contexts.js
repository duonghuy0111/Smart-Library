import { createContext } from "react";

export const MyUserContext = createContext();
export const MyCartContext = createContext(); // Thêm dòng này vào
export const ChatContext = createContext();